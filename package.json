{
  "name": "swabichat",
  "version": "0.0.1",
  "description": "for hiring consideration @ GUILD",
  "repository": "git@github.com:talkshirty2me/mvp.git",
  "author": "swabisan <michaelrogerswanson@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "lerna run prepare",
    "build": "lerna run build",
    "clean": "lerna run clean --stream",
    "dev": "yarn start:dev",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "start:dev": "yarn clean && lerna run start:dev --stream --parallel",
    "start:prod": "yarn clean && lerna run start:prod --stream --parallel",
    "test": "lerna run test --stream"
  },
  "dependencies": {
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.23.0",
    "@typescript-eslint/parser": "^4.23.0",
    "eslint": "^7.26.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "kill-port": "^1.6.1",
    "lerna": "^4.0.0",
    "prettier": "^2.3.0"
  }
}
