{
  "name": "swabichat",
  "version": "0.0.1",
  "description": "for hiring consideration @ GUILD",
  "repository": "git@github.com:talkshirty2me/mvp.git",
  "author": "swabisan <michaelrogerswanson@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "lerna run prepare",
    "build": "lerna run build",
    "dev": "yarn start:dev",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "start:dev": "lerna run start:dev --stream",
    "start:prod": "lerna run start:prod --stream",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
